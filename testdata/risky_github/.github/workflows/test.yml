name: Test
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@main
      - name: Install
        run: curl https://get.something.com/install.sh | bash
      - uses: some/action@latest
